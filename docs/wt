已修掉两条最关键的真实故障链，并把回归测试补上了。
这次落地的核心修复是：
Plan 模式里 AskQuestion 确认后不再因为“没有结构化 answers 数组”被误判成还在等回答，现在线路会正确进入 answers_collected -> fresh explore -> CreatePlan。相关改动在 [plan-workflow.js](E:/project/CursorPool_Client_desktop/js/mode/plan-workflow.js), [plan-mode.js](E:/project/CursorPool_Client_desktop/js/mode/plan-mode.js), [cursor-relay-runner.js](E:/project/CursorPool_Client_desktop/js/utils/cursor-relay-runner.js)。


这次先收口了你说的 plan -> build -> agent 回归，不再让它因为 Build 之后的 RunSSE/requestId 切换把前端会话直接丢掉。现在 execute_plan_action 不会立刻在旧的 plan 流上硬恢复 agent，而是先把旧 session 保留成“等待新 RunSSE 接管”的短暂 handoff 状态；如果新的前端流很快连上，就把执行接过去；如果没连上，再做本地兜底恢复。这样就能避免“后端还在跑，但 Cursor 前端已经结束空白”的分叉。相关修改在 [js/utils/cursor-relay-runner.js](E:/project/CursorPool_Client_desktop/js/utils/cursor-relay-runner.js)。
我还补了回归测试，直接覆盖了 Build -> agent 这条链上的几件事：等待重绑的 session 还能被 stable conversation id 找到、run_request execute_plan 可以复用它、旧 RunSSE 关闭时不会直接 abort。验证已通过：
node scripts/test-plan-ask-resume.cjs
node scripts/test-round2-native-task-ui.cjs
node --check js/utils/cursor-relay-runner.js
node --check js/mode/plan-mode.js


这轮我把重点先落在你现在最影响使用的两类问题上了，核心都在 [cursor-relay-runner.js](E:\\project\\CursorPool_Client_desktop\\js\\utils\\cursor-relay-runner.js) 和 [test-plan-ask-resume.cjs](E:\\project\\CursorPool_Client_desktop\\scripts\\test-plan-ask-resume.cjs)。
现在修掉的是：前端 RunSSE 意外断开时，不再默认把后台还在跑的 agent session 直接 abort 掉，而是先 detach 保活，后续按 stableConversationId 重新接回；同时完成态缓存也补了稳定会话键，避免像 9dbfd881... -> 81d58062... 这种续上的新 requestId 拿不到旧轮结果。另一个是流式文本我加了很短的本地缓冲批量刷出，减少一小粒一小粒往外蹦的卡顿感，但没有伪造 think 卡片，因为你说得对，那种“Let me...”在这批日志里本来就是上游普通 text，不是 reasoning 丢了。
我跑过的校验都通过了：
node --check js/utils/cursor-relay-runner.js
node scripts/test-plan-ask-resume.cjs
node scripts/test-round2-native-task-ui.cjs
这次新增回归里已经覆盖了：
build -> agent handoff 的 runsse_closed 不再误 abort
运行中的 active relay 被前端断开时改为 detach 保活
新 requestId 可以靠 stableConversationId 找回旧 session
completed turn 可以跨 requestId 回放
还需要你在真实前端再重点复测两条：
复现一次你说的 agent 后台还在跑，但前端直接丢失结束，看现在是否会自动续上原流而不是重开新对话
观察 b60e6d3f... 这类输出是否顺滑一些；如果你还觉得颗粒太碎，我下一轮可以继续调 visibleTextFlushMs/visibleTextFlushChars

